import { Inter } from 'next/font/google';
import { Lusitana } from 'next/font/google';

const inter = Inter({ subsets: ['latin'], display: 'swap' });
const lusitana = Lusitana({ weight: '400', subsets: ['latin'], display: 'swap' });

export { inter, lusitana };